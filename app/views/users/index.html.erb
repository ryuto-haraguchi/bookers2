<%= render 'user_info_create_book', user: @user, book: @book %>

<div>
  <h2>Users</h2>
  <p>image</p>
  <p>name</p>
  <% @users.each do |user| %>
  <%= image_tag user.get_profile_image(100, 100) %>
  <ul><%= user.name %></ul>
  <ul><%= link_to "Show", user_path(user.id) %></ul>
  <% end %>
</div>